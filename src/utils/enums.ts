export enum MetadataKeys {
  BASE_PATH = 'base_path',
  ROUTERS = 'routers',
  AUTHORIZE = 'authorize',
  AUTHENTICATE = 'authenticate',
}

export enum HttpMethods {
  GET = 'get',
  POST = 'post',
  PUT = 'put',
  DELETE = 'delete',
}
